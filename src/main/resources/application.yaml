server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/aqi_db
    username: postgres
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  redis:
    host: localhost
    port: 6379

jwt:
  secret: verySecretKeyChangeLater
  expiration: 3600000
